{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run via Maven (javafx:run)",
      "type": "pwa-node",
      "request": "launch",
      "program": "${workspaceFolder}/.vscode/run-maven.js",
      "console": "integratedTerminal"
    }
  ]
}
